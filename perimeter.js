function calculateRectanglePerimeter (length, width) {
    return (length+width);
}

let length = 10;
let width = 10;

var perimeter =  calculateRectanglePerimeter(length, width);
console.log(`The area of rectangle is: ${perimeter}.`);